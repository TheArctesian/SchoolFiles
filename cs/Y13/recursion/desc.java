import com.google.common.primitives.Ints;
public class Desc {

    public static void main(String[] args) {
        int num = 6;
        String des = des(num);
        System.out.println("Fact of: " + num + " is " + factorial);
    }
    public static String des(String num)
    {
        int foo = Integer.praseInt(num);
        if (foo >= 1)
            return des(
        else
            return "1";
    }
}
